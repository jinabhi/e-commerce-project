// import { DesktopOutlined } from "@ant-design/icons";

import { baseRoutes } from "../../../helpers/baseRoutes";

const accessRoute = {
  NOTIFICATION: {
    path: `${baseRoutes.sellerBaseRoutes}/notifications`,
  },
};

export default accessRoute;
