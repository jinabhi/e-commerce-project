import { baseRoutes } from "../../../helpers/baseRoutes";

const accessRoute = {
  MY_STORE: {
    path: `${baseRoutes.sellerBaseRoutes}/my-store`,
  },
  SUBCATEGORY_PRODUCT: {
    path: `${baseRoutes.sellerBaseRoutes}/subcategory-product`,
  },
};

export default accessRoute;
